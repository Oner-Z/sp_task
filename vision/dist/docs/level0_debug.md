# Level 0: ROS 2 通信机制与 QoS 调试

## 背景知识

这道题的目标就是让你快速上手，理解什么是ros。抛开复杂的概念，ros本质上完成的任务就是便利的进程间通信。比如，我有两个进程，一个进程发布雷达数据，另一个进程接收。使用ros就可以方便的完成通讯。你可以搜索以下，发送信息有哪些类型，分别有何特点。特别注意，不同的信息传输方式有不同的质量要求。你不会允许送的外卖没到你手上，但是一个电话过来，也许漏接了也无所谓，可能只是个诈骗。ros2也是这样。重点关注这一点会对这道题有所帮助

## 任务目标

使用任何你想到的方式，修复 `qos_debugger` 节点中的错误，并添加 FPS 计算和消息发布功能。目前qos_debugger运行后是没法正常接受点云数据的

## 问题描述

当前的 `qos_debugger_node.cpp` 存在问题：

1. **代码错误**：没法正常接受点云数据
2. **缺少 FPS 计算**：只打印时间戳，没有计算帧率
3. **缺少消息发布**：没有发布诊断信息到 `/sensor_monitor` 话题

## 输入接口

- **话题**：`/livox/lidar`
- **消息类型**：`sensor_msgs/PointCloud2`
- **发布频率**：约 10 Hz

## 输出接口

- **话题**：`/sensor_monitor`
- **消息类型**：`robot_msg/LogMsg`
- **字段**：
  - `header`: 标准 ROS header
  - `fps`: 计算得到的帧率
  - `node_name`: 节点名称 "qos_debugger_node"
  - `diagnostic_info`: 诊断信息字符串

## 实现要求

### 1. 修复代码

**请在这里展示你的修复方式**

### 2. 添加 FPS 计算逻辑

**请在这里展示你的新增代码**

### 3. 发布诊断消息

**请在这里展示你的新增代码**

## 验证方法

**请在这里展示你如何确定已经完成了level0**

预期/sensor_monitor输出应该包含：
- `fps` 字段约为 10.0
- `node_name` 为 "qos_debugger_node"
- `diagnostic_info` 包含状态信息


## 提示

- 代码错误只有一句
